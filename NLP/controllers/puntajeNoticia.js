'use strict'
// Modulos

// Modelos

// Servicios

// Metodos publicos
function machinel(req, res) {
    res.status(200).send({
        message: 'Funciona wiiiiii'
    })
}

// Metodo privados

module.exports = {
    machinel
}